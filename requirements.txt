numpy
# Optional: install torch for supervised guidance or demos (`pip install torch`)
# Optional: install Kompute Python bindings for experimental GPU backend.
# Prefer source install if PyPI wheel/build fails: `pip install git+https://github.com/KomputeProject/kompute.git`
